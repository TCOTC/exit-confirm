退出快捷键：

- Windows：<kbd>Alt</kbd> + <kbd>F4</kbd>
- macOS：<kbd>Command</kbd> + <kbd>Q</kbd>

p.s. 由于作者不清楚 Linux 的退出快捷键是什么，所以不支持 Linux。

弹窗交互：

- 取消退出：按 <kbd>Esc</kbd> / 点击取消按钮
- 确认退出：再按一次退出快捷键 / 点击退出按钮
